apiVersion: v1
kind: Service
metadata:
  name: adservice
  namespace: ecommerce
  labels:
    app.kubernetes.io/name: adservice
    app.kubernetes.io/part-of: ecommerce-production-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: adservice
  ports:
    - name: grpc
      port: 9555
      targetPort: grpc
      protocol: TCP
