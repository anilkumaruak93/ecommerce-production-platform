apiVersion: v1
kind: Service
metadata:
  name: emailservice
  namespace: ecommerce
  labels:
    app.kubernetes.io/name: emailservice
    app.kubernetes.io/part-of: ecommerce-production-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: emailservice
  ports:
    - name: grpc
      port: 8080
      targetPort: grpc
      protocol: TCP
