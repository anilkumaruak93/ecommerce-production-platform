apiVersion: v1
kind: Service
metadata:
  name: paymentservice
  namespace: ecommerce
  labels:
    app.kubernetes.io/name: paymentservice
    app.kubernetes.io/part-of: ecommerce-production-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: paymentservice
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
      protocol: TCP
